name: Supabase Keep Alive

on:
  schedule:
    # 5日に1回（無料枠対策）
    - cron: "0 0 */5 * *"
  workflow_dispatch: # 手動実行も可能

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Edge Function
        run: |
          curl -fsS https://cfjamhokpbqeufhjoqje.supabase.co/functions/v1/ping
